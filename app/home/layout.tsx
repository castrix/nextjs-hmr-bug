'use client'

export default async function HomeLayout({ children }: { children: React.ReactNode }) {
  return (
    <div>
      <div>change this line when the project is runninghhh</div>
    {children}</div>
  )
}
